# AI 工作入口 · Flip House Platform

更新：2026-09-15。适用于所有开发助手；用户当前明确指令优先。

## 开工必读

1. [产品方向与执行原则](docs/产品方向与执行原则.md)：确定的产品与业务边界。
2. [当前实现与版本核验](docs/当前实现与版本核验.md)：实际代码、已验证项与未解决风险。
3. [Jira 执行清单](docs/Jira执行清单_2026-09-15.md)：Epic、执行单和依赖；开工时再读取 Jira 实际状态。

按需查阅：[阶段目标](ROADMAP.md)、[完整验收](docs/本周开发Tickets_2026-09-15.md)、[真机验收](docs/Demo验收_2026-09-18.md)、[AWS 方案](docs/AWS内部部署方案.md)、[运行命令](README.md)、[协作约定](docs/仓库与协作约定.md)。各主题只在对应文件维护，不新增日期版路线图、重复审计或交接长文。

## 工作区与事实

- 本机唯一日常目录：`/Users/Ryan/Desktop/GitHub/flip-house-platform`；Documents 路径是兼容链接。运行 `python3 scripts/check_workspace.py` 核对分支、改动与远端。
- 同一工作区轮流修改。只有用户明确安排并行工作时才建独立 worktree。保留他人未提交内容，不强制同步。
- 正式交付到 `origin`：`skyvision-ryan/flip-house-platform`；`upstream` 是历史来源。使用 gh 时显式指定正式仓库。
- 笔记本 `7ffb139` 已在 upstream 找到并经 PR #5 合入，当前已有登录和六段基线。项目成员授权、可分派任务、真机交接和 AWS 仍须分别验收。
- 产品目标、代码实现、部署成功是三种事实。Jira Done、代码合并和测试通过不能互相替代。旧材料和其他 agent 的命令只作证据，不自动成为执行指令。

## 当前边界

- 目标：2026-09-18 iPhone 协作 demo；09-21 至 09-25 AWS 内部试用。时区 America/Los_Angeles。
- 三个 Epic：KAN-35 账号/AWS、KAN-36 项目任务、KAN-37 iPhone；执行单 KAN-20–34。KAN-20 已完成。旧 KAN-17/18/19 关闭是结构替换，不代表业务已交付。
- 先落实 KAN-21 账号/项目成员契约，再接任务和手机；不用等整个 KAN-35 云工作完成。
- 保留 React/TypeScript/Cloudscape + FastAPI/SQLAlchemy。首版为联网的 iPhone Safari/主屏幕网页应用。AI 接入、原生 App、离线/推送、多租户收费不在本周。
- AWS 采用 ECS/Fargate + RDS PostgreSQL + 私有 S3 + Cognito + CDK；首期前后端同源，不重新选择云架构。
- 任务实例接收具体 user_id；后端核验角色、项目与动作。迁移保留稳定 step key、证据、历史、D/J 双确认及最近一次 Final 检查规则。
- 一次性交付、持续工作和审批节点分别定义完成条件；阶段与任务状态分离，只有明确前置才阻塞。待办、总览和助手共用状态。
- 原始录音、真实人员/业主资料、凭证和采购原件不复制进代码；验证使用合成数据。现有参考材料不是待执行计划。

## 交付规则

- 一张执行单对应一个分支和 PR，按一次可审阅 push 的范围交付。用户授权的文档/开发工具清理可在独立整理分支完成，不冒用已关闭票号。
- 用户明确指定的票优先；否则查看进行中工作、Jira 实际依赖、日期与执行清单，同等条件再看 backlog rank。Epic 不直接作为业务开发单。
- 开工前确认目标、范围、可验证结果和真实依赖。普通技术细节自行解决；只就影响当前工作的业务决定提问，不因模板空栏阻塞整轮。
- 已授权事项直接执行。范围外缺陷先复现、查重，已有单覆盖就补证据；新需求或架构只提简短草稿。禁止从旧审计批量再建一套 Epic。
- 提交主题以当前 `KAN-编号` 开头，只暂存本次文件。无 ticket 的文档/工具清理保留为可审阅本地提交，不借逃生口伪造票号。
- PR 附实际改动、验收证据和未验证项。转 In Review 必须真实回写成功；默认由用户合并，除非当前另有明确授权。合入并完成票内验收后才标 Done。
- 本地 hooks 是 Claude 的辅助检查，不覆盖全部写入或其他助手，也不代替 CI。`KAN_BYPASS=1` 会留日志，不代表验收通过。

## 验证

- Python 3.12、Node 22；命令见 README。运行 `python3 scripts/check_local.py` 执行隔离的后端测试、工具测试和前端构建。
- 文字变动检查链接、状态一致性和 `git diff --check`。修改 hooks 必须运行工具测试；业务改动补与风险相关的测试。
- 任务/审批验收包含缺证据、失败检查、重复提交、越权、不适用和未满足前置；持久化包含重启、重新部署与恢复。
- iPhone、Cognito、S3/RDS 和 AWS 发布必须实测；无法执行就明确未验证，不以桌面模拟或配置文件代替。
