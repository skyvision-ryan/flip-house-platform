# 给正在运行的 Claude：核对最新进展后调整剩余工作

日期：2026-09-15，America/Los_Angeles。

> 历史交接记录：此后已找到 `upstream/main@7ffb139`，PR #5 已合入 main。唯一日常入口已选定 Desktop/GitHub，以下早先的本地状态不能当作当前事实；以[版本核验](当前实现与版本核验.md)和[协作约定](仓库与协作约定.md)为准。

## 交接来源与边界

用户已要求 Codex 直接向终端里的 Claude 传达本次审计与调整意见，并提醒我们看到的计划可能已落后于你的执行进展。终端控制工具拒绝访问，故改用本文件交接。

**先核对你当前会话的实际进展，不要把旧计划或 Codex 的本地状态当作你的实时状态。保留已经完成且有价值的工作，不重做，不回滚。** 本文件是本次交接意见；既有产品事实和任务范围继续引用下方权威文档，不新增另一套路线图。

## 先对齐事实

1. 简短说明你当前机器、仓库路径、分支、HEAD、未提交改动、正在执行的票号，以及已经完成的功能或流程建设。不要打印凭据。
2. 读取 [AGENTS.md](../AGENTS.md)、[CLAUDE.md](../CLAUDE.md)、[ROADMAP.md](../ROADMAP.md)、[版本核验](当前实现与版本核验.md)、[Jira 执行清单](Jira执行清单_2026-09-15.md)。核对当前 Jira 和代码，区分计划、实际实现、已验证和已部署。
3. Codex 本轮读到的本地分支是 `codex/mobile-demo-roadmap`，HEAD 为 `ed4493a`。其中已有 `backend/tests/test_utility_websites.py`，CLAUDE.md 已指向新交付安排，本机也已有 Node/npm。这些只是本机时间点事实，不能据此断言另一台电脑的情况。你的原计划中“没有任何测试、没有 Node、给旧 CLAUDE.md 加第 20 节”等前提需要重新核验。
4. 文档记录已有 KAN-17/18/19 与 KAN-20–34。以 Jira 实际状态复核，不重复建单，不预设新票号为 KAN-16。其他设备的未同步实现应比较并复用，避免覆盖用户或其他助手的工作。

## 调整剩余工作

- 首要结果仍是 **9 月 18 日 iPhone 真实任务交接演示**，以及 **9 月 21–25 日 AWS 内部试用**。账号、任务、手机交接的可验证成果优先。
- 如果开发流程工具已经做好，保留并使用；如果仍在建设，只完成当前交付必要的部分。不要再把完整 hooks、全部 Jira 快捷命令、历史审计导入和自制 Jira 客户端作为所有业务开发的前置条件。
- 保留最小保障：可运行环境、票号关联、简洁 PR 说明、相关测试、前端构建，以及可审阅的验证证据。复用现有测试，不为了流程统一先整体迁移框架。Jira 连接受阻只影响在线同步，已明确范围的本地开发继续推进。
- 按已完成证据和显式依赖，选择下一个可执行子票。现有起始安排为 KAN-20 → KAN-21 → KAN-27；若已完成则核验证据后继续后续票，绝不从头重做。不能仅按 backlog rank 选择父票、被阻塞的票或等待审查的票。账号契约通过后即可联调任务和手机，不等待整张云部署父票关闭。
- 每张小票交付一个范围清楚、可验证、可回退的增量。提交和 PR 带子票号，必要修复可以追加提交；范围过大再拆分，不机械强求一次 push。普通工程选择自行完成，只提出真正阻塞当前工作的业务问题。

## 需要纳入当前权限交付的审计发现

Codex 在上述本地版本中读到：`backend/app/routers/common.py` 的 `get_actor` 在缺身份时默认负责人；`backend/app/routers/ops.py` 的水电读取接口没有身份/项目权限依赖，`UtilityOut` 含 password；现有水电测试还断言无身份请求能返回测试密码。这是代码证据，不是对线上真实泄露的确认。

请先在你当前版本复核：若已修复，提供验证证据；若仍存在，纳入 KAN-21 或关联修复票。同步修正冲突的旧测试，覆盖未登录、越权和跨项目访问。不得将这些上线阻断问题仅标为预期失败后宣布可内部上线。用合成数据验证。

## 收尾要求

- 每次完整改动运行相关测试，交付前完成必要总检查；不必每写一个文件就运行完整回归。
- 工具端点、运行时支持版本、hooks 和权限机制按当前官方文档及本机实际版本核实。不要把未经验证的命令配置当作已经生效的保障。
- Jira 如实区分进行中、待审查、已完成；提交、合并、部署是不同事实。父票另做整体验收，真机和 AWS 验收要有实际证据。
- 请先简短回复：你当前已做到哪里、本交接哪些判断已过时、剩余计划如何调整。随后继续当前第一个可执行子票，不停留在重新写一份长计划。
- 后续汇报只需：完成结果、票号与代码链接、验证证据、尚未完成或受阻事项、下一步。
