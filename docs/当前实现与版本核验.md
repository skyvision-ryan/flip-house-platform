# 当前实现与版本核验

核验日期：2026-09-15。本页区分已读代码、用户提供的其他设备记录与目标能力。它是时间点记录，后续交付须补充新的提交与验收结果。

## 1. 已核验来源

- 本仓库：`skyvision-ryan/flip-house-platform`；核验时 HEAD 和 `origin/main` 都是 `0a459c1`，工作区干净。
- 早先只执行 `git fetch origin`，未找到 `7ffb139`。**同日更正**：Desktop 克隆还配置了 `upstream = https://github.com/LianCr/flip-house-platform.git`；`upstream/main` 正是 `7ffb139`。Documents 克隆现也已添加并拉取该远端。
- 笔记本整理对应的 `7ffb139` 代码来源已取得。其代码包含 auth/settings、登录与用户管理页面、六段清单及水电密码分级；具体边界和迁移仍需集成测试，不根据文件存在就宣布 KAN-20/21 完成。原始附件不复制进仓库。
- 本轮只修改项目文档与开发计划，没有实现下表中的目标功能，也没有操作在线服务。

## 2. 能力差异

| 能力 | 当前仓库核验 | 笔记本整理所述 | 后续处理 |
|---|---|---|---|
| 阶段 | `dictionaries.py` 五段清单 | 六段 31 项，含六道双确认门及上市证据门 | 代码已取得，集成时核对；保留 key 和历史，记录六段映射，不仅按数量验收 |
| 用户与登录 | 无 users/auth/settings；`get_actor` 接受 X-Actor，默认负责人 | 独立账号、管理员管理、签名 cookie、正式模式拒绝自报身份 | T1 优先复用；没有实际代码不能标为已实现 |
| 流程与待办 | StepsPanel、MyTodo、按角色 dashboard、文件/检查/采购、更新日志已存在 | 同类能力已有进一步修改 | 在统一基线上扩展，不重复建立另一套状态 |
| 项目任务 | ProjectStep 保存 key、done、操作人、日期和备注；无独立可分派 Task 实例 | 工作流引擎仍列为上线后计划 | T2 新增任务实例和最小完成规则 |
| 手机协作 | React/Cloudscape 页面；无已验证的完整手机任务交接 | 尚无完整手机端执行验收记录 | T3 用手机宽度与两台设备验证 |
| 存储 | SQLite + DATA_DIR/uploads；空库启动自动灌演示数据 | Postgres/S3 列为待做 | 下周内部上线前实现持久化、迁移和禁止生产自动 seed |
| 助手 | AssistantPanel + insights 规则生成；无实际 LLM 调用 | 同为规则助手，提出工具与确认卡方案 | 保留诚实标注，后续按能力分期 |

直接代码入口：[models.py](../backend/app/models.py)、[dictionaries.py](../backend/app/dictionaries.py)、[common.py](../backend/app/routers/common.py)、[db.py](../backend/app/db.py)、[main.py](../backend/app/main.py)、[App.tsx](../frontend/src/App.tsx)。

## 3. 环境不能混为一谈

- 当前 README 记录的 Render demo：`flip-house-platform-ryan.onrender.com`，main 推送触发部署，环境是演示数据与临时存储。
- 笔记本附件写的是另一个地址：`flip-house-platform.onrender.com`。本轮没有核验该站点的代码、账号或运行状态，不能视为同一个环境。
- AWS 是 2026-09-21 至 09-25 的目标环境，本轮未创建资源。旧材料称 AWS 账号待开通，目前状态待核实。

## 4. 同步与迁移规则

1. KAN-20 集成已取得的 `upstream/main@7ffb139`，保留 `origin/main` 的水电 website 字段、链接界面、迁移及测试。两个分支相对共同祖先各有新增，文件比较中的缺失不应直接被解释为对方有意删除功能。
2. 笔记本代码来源已经落实，不再按“来源无法取得”另写一套登录或六阶段功能。由一个实现者负责 KAN-20 合并，其他助手避免在同一工作区并发修改；未提交的流程工具单独保留。
3. 项目历史不随模板重排重算为虚构完成；不得把旧“筛选完成”复制成所有新增尽调都完成，或把旧采购 done 复制成全部到货。
4. 任务迁移提供可重复执行且不会重复生成任务的机制；保留旧记录和来源映射，失败能回到迁移前状态。
5. 核验记录必须说明：提交、测试命令/场景、已完成范围、未验证项；部署另记录环境与版本。

## 5. 当前仍需落实的外部输入

首批真实账号与角色/项目分工；AWS 账号、权限、region、资源预算；2–3 套试运行项目资料。缺失输入只影响相关实施，不阻止文档、合成数据 demo 和独立功能开发。

## 6. 本次仓库对齐发现

- 同名的 Documents 与 Desktop 文件夹是两个独立克隆，未提交文件和本地分支不会自动互通。
- 正式交付远端为 `skyvision-ryan/flip-house-platform`；笔记本来源远端为 `LianCr/flip-house-platform`。
- 本次先统一来源、产品指引和协作方式；Claude 正在 Desktop 的 KAN-20 分支进行代码集成。是否完成以该分支最终提交、测试与 PR 为准，不将正在进行的合并写成已完成。
- 日常入口及同步方法见[仓库与协作约定](仓库与协作约定.md)。
