# 当前实现与版本核验

核验日期：2026-09-15（整篇审计）。**2026-09-17 增量核对**：仅核对当天合入的改动与 09-17 会议带来的缺口，其余条目的验证日期保持 09-15，未重新执行。**2026-09-18 文档核对**：只记录 AI 三层基础的当前边界，未把开发配置误写成业务能力。**2026-09-21 增量核对**：见下节；只核对合入记录、Jira 状态与汇报口径，未重新跑全部验证。

当前审计基线为 main 的 `73a01c2`（PR #7）；业务代码基线来自已合入的 [PR #5](https://github.com/skyvision-ryan/flip-house-platform/pull/5)，合并提交 `5965b0a`。09-17 的 main 已前进到 `9922d5c`。

## 版本来源

- 正式仓库：`origin = skyvision-ryan/flip-house-platform`。
- 笔记本来源：`upstream = LianCr/flip-house-platform`，提交 `7ffb139`。
- 早先只查 origin 导致漏查；该提交已经找到并完成统一，不再按“代码不可取得”重复开发。
- 本机唯一实际目录是 `/Users/Ryan/Desktop/GitHub/flip-house-platform`；Documents 路径是兼容链接。

## 当前代码包含什么

| 范围 | 已核实 | 尚未完成 |
|---|---|---|
| 阶段与证据 | 六段 31 项；旧 29 个 step key 保留；一段可多门；D/J 双确认；最近一次 Final 检查必须通过 | 任务实例、持续工作完成规则、历史项目迁移实测 |
| 身份 | 账号密码、签名 Cookie、管理员用户管理；正式模式未登录与伪造 X-Actor 均拒绝 | 项目成员、项目级授权、稳定 user_id 契约、真实账号联调 |
| 水电 | 网站、网址校验、跳转/复制；密码按角色隐藏 | 项目权限接入及正式环境验证 |
| 项目工作台 | 清单、角色待办、文件/检查/采购、更新记录、预算与确定性分析 | 具体账号分派、任务状态/原因、证据审阅、管理端同源 |
| 项目总览 B 区 | **09-17 合入**（KAN-44，PR #12，`9922d5c`）：按当前阶段 / 我负责的 / 关键节点 / 其余事项分组；11 种状态只陈述后端事实并给出依据；附件区分 `step_key` 精确绑定与按 `doc_type` 的项目级资料；31 个模板项带 `purpose` / `done_when` 静态文案 | 跨项目概况页、周期对照的整体视图 |
| 工期与事项两条横条 | KAN-46（PR #14）**09-21 前已合入 main** | 线上版本以部署后的健康检查为准 |
| 手机 | React/Cloudscape 响应式页面、登录页面 | iPhone 任务列表/详情、上传、主屏幕及双设备交接 |
| 数据与文件 | SQLite、本地 uploads、DB_URL 与存储配置入口 | PostgreSQL 驱动和版本迁移；S3 实现；部署与恢复 |
| 助手 | 本地规则洞察和页面链接；开发工作区可见三个项目级 Agent Skills（包括新项目启动检查）与 Atlassian（Jira）MCP 连接配置，但它们均不在业务应用运行路径中 | 权限一致的产品工具层、其余业务 Skills、来源引用、确认写入；未实现产品级 MCP server、生成式 LLM 或 Jev 式决策层 |

## 2026-09-24 全员演示合并前检查（KAN-75 / PR #28）

- **本轮范围**：单一“设计方向”入口；T/admin 全部、D 董事及以下、J Jessie 及员工、其余本人职责。后端目录和详情均使用实际账号授权，旧路径跳转保留；原设计比较、参考来源和反馈不删除。设计预览自己的合成切面不写业务数据。
- **三房演示**：可选 `prepare_team_demo.py` / `start_team_demo.py`，Cedar s1、Oak s3、Pine s5；完整 31 节点，每房 40 项任务，八人各有至少 6 项职责事项。默认 dry-run，显式 apply 先备份 SQLite / uploads 并用临时副本恢复校验；只允许明确的旧项目 ID 迁移，拒绝跨范围引用。幂等重跑保留账号、密码、任务与操作；旧项目 / 不完整演示启动失败，不静默清库。真实名单和密码仅从私有文件 / 环境输入，不进入 Git。
- **检查中修复**：任务更新使用版本条件写入，真实并发失败回滚并返回 409；任务列表 / 详情 / 历史 / 成员和个人任务按真实账号的全局权限或有效项目成员读取；正式模式房产详情 / 检查列表补登录依赖。员工文件登记只提交有权更新的字段，报错可见；跨任务上传草稿销毁。Sabrina 预算可用，分析页入口与旧链接按实际分析权限处理。
- **验证证据**：`python3 scripts/check_local.py` 六步通过：后端 148、前端 142、开发工具 17、脚本工具 24 项，构建与 diff 通过。新增隔离系统测试覆盖八账号交接闭环、同角色隔离、财务 / 文件访问、三项目完整节点与阶段推进；并发、生成器备份恢复、启动幂等各有独立回归。浏览器实测八账号设计层级、Zoey → Jessie 文件交接、Sabrina 支出汇总、跨房草稿清理和 390×844 窄屏。真实 uvicorn 重启后旧 Cookie 与两项已保存操作保留；空 DATA_DIR 重建后八账号 / 三房 / 120 任务恢复，先前浏览器操作不混入初始数据。
- **线上边界**：Ryan 确认 Render My Workspace、现有 Free 实例，允许丢存储后恢复模拟数据。线上旧资料已作 Git 外 API 导出（10 项目 / 168 附件，无导出错误），不冒充完整数据库备份。检查时线上仍 `d1fd4d0`，`auth/mode` 确认尚无用户账号；未合并部署，未修改线上账号 / 项目。没有升级付费、改邮件或改 VP 服务。
- **仍未验证**：iPhone 真机、线上新版本与恢复、AWS / PostgreSQL / S3 / Cognito。其余旧业务接口的全站项目授权仍属 KAN-21；此次不能称正式业务数据权限和团队试用全部完成。演示步骤见 [验收脚本](Demo验收_2026-09-18.md)。

## 2026-09-24 角色设计比较（KAN-75，同 PR）

- `/design-directions`（旧 `/design-collaboration` 跳转）按实际工作提供完整可点击界面：Jessie 比较 Linear 单房工作台 / Asana 房屋统筹台 / Jira 跨房业务清单；Kody 比较 Carbon 服务核对台 / Linear 行动队列 / Fluent 服务公司办理台。每种都有官方来源、借鉴点、对本项目的调整与取舍，沿用已认可的轻量 B 视觉。
- 同一批合成房屋和资料支持切房、业务页签、搜索筛选、详情、文件预览与账户卡点编辑预览；不写实际项目、任务或审批，不修改 D/J 业务控制。偏好与意见只保存在当前浏览器，按真实账号 ID 和角色设计分开，可复制给 Ryan。
- **真实账号访问控制**：`GET /api/design-workspaces` 仅返回可访问目录；详情接口先 `require_user` 与角色授权，再加载对应方案。`is_admin` 可看全部；D 看董事及以下，J 看 Jessie 及员工；项目助理只看 Kody；采购只看采购；Permit/设计只看 Zoey；财务只看 Sabrina。普通其他角色没有专属设计。未登录 / 停用 401、跨角色 403、未知设计 404；演示 `X-Actor` 不扩大权限。
- 设计内容由后端鉴权后下发，Kody 只收到自己的方案与事项；不是前端把全量角色设计隐藏。普通员工直接打开旧通用设计比较会回到自己的角色设计。采购、Zoey、Sabrina 已提供三方案比较，各自独立 JSON 在授权成功后才加载；David 与 T 的三方案比较也已开放；T 沿用 admin 身份判断，不新增角色或账号，普通账号名为 T 或 001 不获得权限。D 默认打开 David，admin 默认打开 T，J 默认打开 Jessie。
- 采购：Carbon 按房采购核对 / Linear 跨房状态队列 / Fluent 材料资料台，共用 3 房 9 项；Tristin / Jeremy 的分工为合成筛选，不声称采购行已绑定真实负责人。Zoey：Linear 单房资料 / Jira 跨房跟进 / Carbon 文件登记表，共用 4 房 9 条记录；申请回执、核发 Permit、历史与最新检查分开，检查与文件的配对为合成预览，不声称已有强关联模型。Sabrina：Carbon 支出账簿 / Linear 待核对队列 / Cloudscape 逐房预算，共用 3 房 6 条记录；已登记支出 $11,470，不包括 $980 未登记单据；预算差额不当利润，付款核实为设计样例状态。
- 本地完整检查通过：后端 109、前端 134、开发工具 17 + 24 项及构建 / diff。19 项设计权限测试覆盖 D/J/admin、职责角色、同采购角色共享、匿名 / 停用 / 伪造身份、越权不加载方案及角色数据隔离；新增偏好校验与 4 项财务测试覆盖已登记 / 未登记分离、负差额、交叉筛选、金额小数精度。
- David：Cloudscape 项目观察表 / Linear 议题与更新 / Power BI 式估算依据探索；支持切房、议题筛选、版本对照、成本与假设来源、项目事实及资料预览。版本售价差额减成本差额等于利润估算差额，不表示期间实际盈亏。
- T：Carbon 项目经营账簿 / Linear 经营问题聚合 / Cloudscape 阶段分布与指标下钻；同一筛选同步汇总、覆盖分母和明细，支持投入构成、收益缺项、逾期与当前待审批次追溯。经营数字仅围绕房屋业务，不是 Jira/VP 软件开发汇报。
- 两人共用 6 套合成房屋：4 套进行中，其中 3 套完整当前模型合计 $83,100、3 套可计算登记投入规模合计 $1,494,000；1 套已售、1 套线索分别展示，不混入进行中汇总。模型复用现有交易分析公式，缺输入不计算，真实零保留；融资前规模不称股东现金占用，已售结算未齐不展示最终净利润，持有日数不当完成度或员工效率。新增后端 4 项与前端 15 项测试覆盖模型、缺失值、汇总、重复项目、日期及待审批次边界。
- David/T 浏览器验收：D/admin 登录默认入口、D 当时的全目录（现按上节收紧）、跨账号设计意见隔离；David 版本差额、缺项提示与来源；T 全量及阶段筛选汇总、估算覆盖、待审事项打开正确房屋与节点；桌面与 390×844 详情返回、无页面级横向溢出均通过。最终文案与版本差额补充后构建通过。
- 本轮使用合成采购、Permit/设计、财务账号验证各自专属入口，采购跨角色链接拒绝；采购人员筛选 / 跨方案编辑、Zoey 最新 Final 与登记表资料匹配、财务单据 / 筛选合计 / 跨方案房屋与备注保持；三组均验证 390×844 详情返回与无页面级横向溢出。
- 浏览器使用独立临时数据库验证 J 当时的全目录（现按上节收紧）、采购筛选 / 单房资料对象一致、Kody 专属入口与跨角色链接拒绝、登录后返回设计页、手机 390×844 详情 / 返回及无页面级横向溢出。此为本地设计比较验收；线上部署、真实员工反馈、真机 iPhone 未验证。邮件不改。

## 2026-09-24 Demo 员工账号（KAN-75，同分支）

- 邮箱登录在比较前去首尾空格并转小写，保留原账号名登录。内部 ID、任务归属与历史不改；密码沿用带随机盐的 PBKDF2-SHA256。
- `scripts/provision_users.py` 从本机 JSON 名单和 `INITIAL_PASSWORD` 导入，按邮箱幂等更新姓名 / 角色；默认不改已有密码，显式 `--reset-password` 才重置。仅创建普通账号，不赋管理员、不自动加入项目、不改名单之外的人。
- Ryan 明确授权增加「采购」「财务」「Permit/设计」三个执行级职责角色；随后确认 Jessie 助理的职责为全局项目概况、水电瓦斯及账户资料、保险，单列为「项目助理」。其他代号保留；D/J 仍走原双确认。角色本身不赋 `is_admin`。

| 角色代码 | 当前权限 / 职责 |
|---|---|
| 老板、D、J | 决策级，可读写财务、创建 / 编辑 / 删除项目、分派、各业务模块和任意文件；仅 D/J 可各自确认对应关键节点 |
| 负责人 | 统筹级上述管理能力，可代 D/J 确认；账号管理接口仍另要求 `is_admin` |
| L、PM | 统筹级，可读写财务、创建 / 编辑项目、分派及业务模块；不可删项目、不可代 D/J 确认 |
| K | 执行级，保险、水电及账号密码、披露资料；没有财务和全局管理权 |
| Z | 执行级，permit 文件和检查；没有财务和全局管理权 |
| S、W | 执行级，卖房文件职责；不等于有金额资料下载权 |
| A | 执行级，园丁相关任务和对应交付 |
| 设计师 | 执行级，设计方案 / 定稿交付 |
| 园丁、承包商 | 外部级，对应步骤的照片 / 交付；文件列表和下载按外部规则过滤 |
| 项目助理（新增） | 执行级，可看全部项目关注、办理水电瓦斯及账户资料、上传 / 更新保险；无财务金额、分派、检查管理或 D/J 确认权 |
| 采购（新增） | 执行级，采购清单及状态；无财务 / 分派 / 全局管理 / D/J 确认权 |
| 财务（新增） | 执行级，金额可见、预算与支出维护、发票上传；无定价修改 / 分派 / 全局管理 / D/J 确认权 |
| Permit/设计（新增） | 执行级，继承 Z 与设计师的文件交付、检查操作；不改变两者旧代号，无 D/J 确认权 |

项目关注按决策 / 统筹 / 项目助理的全局视图，或其他执行人员的有效项目成员关系过滤；具体任务由被分派的 `user_id` 执行，审核仍由指定审核人执行。旧模块尚未全面接入项目级授权，本轮不将角色补齐声明为 KAN-21 完成。

- 本次补充隔离 API 回归：项目助理可看无成员关系项目概况、保存水 / 电 / 瓦斯账户并读取账户资料、上传 / 更新 / 下载保险；金额被脱敏，财务、分派、D/J 确认及非保险文件上传被拒绝。变更角色和重复导入保留账号 ID / 密码。
- 员工头像统一为圆形、姓名首字母、固定内部 ID 对应的身份色；不是恢复旧角色色圈，既有字体、邮件、demo/seed 不改。
- 原有演示账号保留，新账号与其并存。种子房屋逻辑本身不创建员工账号；已有测试密码在公开 Git 历史中出现过，不把 demo 密码当正式凭证。
- 本段的本地检查 / 浏览器 / 导入证据以本轮交付记录为准；Render、真机与线上账号导入未验证。安全名单不入 Git，命令见 README。

## 2026-09-23 增量核对（KAN-75 块 1 / 2 / 4 / 5 / 6，分支 `KAN-75-jessie-collab`，未合入）

- 新增任务实例层（KAN-21/27/28 的最小切片，**不等于这三张票完成**）：`users.email`；`project_members`、`tasks`、`task_events` 三张表（`models.py`）；`ensure_tasks` 按模板给每套房建 24 项普通任务实例（7 个关键节点不建，仍走 `project_steps` 的 D/J），建项目、seed、启动后遍历三处调用，幂等靠 `(project_id, step_key)` 唯一。
- 接口（`routers/tasks.py`）：`GET /api/projects/{id}/tasks`、`GET …/members`、`GET …/tasks/{tid}/events`、`GET /api/me/tasks`；写接口 `POST …/tasks/{tid}/assign`（分派 / 改派 / 改截止，改派必填原因，非成员要显式 `join_project`）与 `POST …/tasks/{tid}/status`（start / wait / resume，只有当前负责人）。写接口一律 `require_user`，`X-Actor` 无效；版本号不符 409 并带最新值。
- 执行状态（未开始 / 进行中 / 等待 / 待确认 / 已完成）与证据派生的「满足」并列返回、并列显示；分派层没有「完成」，阶段不变。改派后执行状态回到未开始，历史事件仍署原负责人。审核人为空时默认记为分派的人（待 Ryan 确认）。
- 前端：项目总览用「任务安排」表 + 右侧「任务摘要」骨架作为唯一任务入口，原 B 区（证据清单、D/J、工期条）收进可展开区；我的事项按登录账号取 `/api/me/tasks`，分「现在可做 / 等待中 / 提前准备」，旧按角色待办收进底部折叠参考；用户页可填邮箱。
- 本地验证：后端 53 项（新增 12）、前端单测 10 文件、构建、`check_local.py` 六步全绿；浏览器走通 J 分派 → A 开始 / 等待 → J 改派 A2（原因）→ A 403 → A2 开始；重启后端后数据仍在。**未验证**：真机、Cognito、PostgreSQL；没设 `SECRET_KEY` 时重启会让所有人掉线（已知，KAN-21/26）。
- 邮件仍是零基础；Render 免费实例禁止出站 SMTP，验收环境定为 Render Pro（块 3）。
- **块 2（同分支）**：新增展示分组 `STAGE_GROUPS`（买房=[s1,s2] / 装修 / 预上市 / 卖房上市 / 售出收尾），`compute_steps` 与 `project_out` 一并返回 `group_position`（买房内分「未购入 / escrow 中」，判据只有 open_escrow、close_escrow 两道门）；底层 s1…s6、31 个 step key、D/J、Final 不动。项目头卡改五格位置条；侧栏「线索」入口移除，`/leads` 跳转到 `/projects?group=buying&sub=pre`（按跟进档位分组，复用 LeadGroups）；工作台三桶（未购入 / 在建 / 收尾）、漏斗、卖出文件小组件都改按分组位置计，不再读旧 `stage` 列。新增 `projects.lead_substage_at_escrow`：open_escrow 双确认成立那一次写快照并记事件（`substage` 列过门后仍被 `sync_legacy_stage` 覆盖，不改旧逻辑；双确认后、下一次读项目前档位仍可改一次，已知窗口）。后端 59 项、前端 106 项单测全绿。
- **块 4（同分支）**：项目头卡三条动态事实（按分组：下一动作 / 目标过户 / 当前重点 / City Final / 挂牌日期 / 交割确认 …，`focus_facts`，只陈述任务表、项目日期、关键节点里已有的数据）；曾提供任务表多选批量分派（已按 Ryan 后续 UI 反馈移除入口，现为点行联动摘要与头像单项分派）；右侧摘要加「去我的事项处理 / 审核」「完整活动记录」入口；新页 `/projects/:id/tasks/:taskId`（图 12：只读时间线 + 摘要 + 处理入口）；登录后工作台改为「项目关注」（`GET /api/me/workbench`：每套房位置、下一动作、行动者、截止 + 待我确认 / 待分派 / 等待回复三个数 + 待我处理卡片），未登录访客仍是原四个数与项目表。「下一动作」规则：待审核 > 进行中（最早截止）> 已分派未开始 > 等待 > 待分派。后端 62 项测试。
- **块 5（同分支）**：`task_submissions` / `submission_files`（批次只引用 `files` 行，不复制）；`POST …/submit`（负责人；文件 / 照片类交付物至少一个文件，其余交说明即可；引用文件必须属本项目且当前账号按 `_can_download` 可访问；重复提交只一个批次）、`…/return`（审核人，原因必填，批次与意见保留，任务回进行中）、`…/confirm`（审核人，任务完成、记 `done_at`，**不补 ProjectStep 手工勾**，证据满足仍由 `compute_steps` 派生并列显示）。「下一动作」把任何段的待审核放最前。我的事项重写为三页签（我的任务 / 待我审核 / 需求待确认占位）+ 共用任务处理组件（详情 / 交付或审核 / 活动记录）；工作台加「最近交接」。后端 66 项测试。邮件回执等块 3。

- **工作台可见范围修正（同分支，Ryan 09-23 授权）**：`/api/me/workbench` 使用真实账号的 `workbench_all_projects` 权限（决策 / 统筹）保留全局视图；其他账号按 `ProjectMember.user_id` 与 `active=True` 取项目。项目关注、四个统计、待我处理与最近交接共用该范围，交接先过滤再取最近六条；停用成员关系后，残留的负责人或审核人关联不扩大工作台范围。保留已走完项目不进关注列表、可见项目历史交接可回看的原行为。此修正只覆盖该接口；旧 dashboard 小组件、项目读接口与 `/api/me/tasks` 未改，不代表 KAN-21 的全站项目授权完成。
- **修正验证**：新增 5 项回归覆盖成员 / 非成员、停用与恢复、伪造 `X-Actor`、残留审核关联、交接过滤顺序、统筹全局视图和同角色个人任务隔离；旧代码上 3 项失败，修正后通过。`check_local.py` 六步全绿：后端 71、前端 106、工具 17 + 24 项，以及构建与 diff 检查。新代码只读现有演示数据库：Jessie 8 套、A2 / B 各 1 套（NW Fisk），待分派分别为 44 / 3 / 3。这是新代码复核，非线上部署验收；现有 8010 进程未启热重载，需重启后使用新代码。

- **块 6（同分支，本地已验，等 Ryan 测试）**：新建项目改为「确认房屋 → 准备任务 → 确认创建」。五个展示分组可预先安排全流程 24 项普通任务的实际负责人和截止日期，7 个关键节点单独只读展示；摘要区分当前安排、后续预安排、待分派与人数。最后一步显式确认将被分派人员加入项目，沿用分派人为默认审核人的现有规则。创建后进项目总览，所有任务仍未开始，未安排项可稍后补；阶段、证据、D/J 与 Final 规则不变。房产来源标识、手动填写、重复房产提示和团队金额明确采用逻辑保留。
- **块 6 保存与授权**：`GET /api/projects/creation-members` 只向有分派权限的登录账号返回最小员工信息；`ProjectCreate` 接收预安排和 `request_key`。新增 `project_creations` 表，对同一创建账号 / 请求键去重；相同内容重试返回原项目，改动内容复用键返回 409。房屋、分析、采购基线、24 项任务、成员及分派事件一次事务保存，任何一步失败全部回滚。禁止关键节点、重复 step key、停用 / 不存在账号与未确认入组；不扩大全站数据可见范围。
- **本轮布局整理（Ryan 同轮授权）**：复用 Cloudscape 组件与颜色令牌；项目头卡突出动态事实和位置条，其余资料折叠；总览表格与右侧摘要按内容宽度收栏，分派 / 勾选 / 行点击各自独立；工作台左右栏响应式；我的事项改为左侧任务卡、右侧处理区，增加项目 / 任务筛选。未改无关小组件、预算采购页面或提交审核规则。
- **块 6 验证**：`check_local.py` 六步全绿（后端 77、前端 109、工具 17 + 24、构建、diff）；新增回归覆盖全流程预安排、真实账号隔离、重复请求、非法输入和中途失败回滚。独立演示数据库上走通 Jessie 三步创建、当前与未来任务分派、同人合并预览与账号读取；8 个并发 HTTP 创建请求只生成 1 套房、24 项任务和各一份分派记录，重启后重试仍返回同一项目。桌面与 390px 窄屏检查新建 / 确认 / 我的事项，无页面横向溢出；总览单项分派、勾选与行摘要互不误触。
- **块 6 边界**：邮件块 3 按 Ryan 要求暂缓，本块仅保留置灰开关与按人员合并预览，明确通道未接通，**没有真实发信或成功回执**。块 7 / 8 未开始。浏览器测试使用 `/tmp/kan75-block6-demo` 的演示库副本及本地 8016，原 8010 进程与业务数据未改；使用新后端须重启相应进程。真机 iPhone、PostgreSQL、Cognito、部署和真实邮件均未验证。

- **KAN-75 全站卡片 UI 整理（Ryan 再次授权，同分支）**：统一容器 / 表格 / 字段与标题层级；字体按 Ryan 后续反馈恢复 Open Sans 正文 / Helvetica Neue 标题与 Cloudscape 默认字号、行高、字重；窄卡片按实际宽度收列。头卡将小图和房屋身份、当前事实、五格位置条分开；任务摘要改标签 / 值行，数据页分离主值 / 来源 / 编辑，预安排、我的事项、工作台、分析、预算、文件及账号表单共用样式。中性底、蓝色交互、克制的橙色主按钮，保留真实状态色。
- **显示与反馈**：顶栏「显示设置」提供独立的辅助说明、卡片编号开关，设备本地记忆；必要风险、来源、约束与任务要求常显。95 类卡片组件使用 1–99 范围的固定注册编号；重复实例通过上下文和路由区分。点击复制后可附截图汇报，不自动建票或发消息。角色色圈、旧偏好和着色函数已删除，角色文字与实际员工的中性字母头像保留；权限未改变。
- **UI 验证**：`check_local.py` 六步通过：后端 77、前端 106、工具 17 + 24、构建及 diff。前端移除 7 项已删除色圈功能的测试，新增 4 项覆盖显示偏好互不覆盖 / 禁用存储、编号范围唯一性 / 固定位置、重复卡片定位。浏览器实测四种开关组合、切页 / 刷新记忆、复制后实际粘贴、行点击联动摘要；关闭说明时等待原因 / 截止 / 证据 / 风险 / 来源 / 重复房屋提示仍显示。桌面走查工作台、项目总览、我的事项、新建及数据 / 分析 / 预算 / 文件；390px 窄屏检查预安排、任务处理与双开总览，均无页面横向溢出。表格自身可横向滚动以保留必要列。
- **本轮视觉反馈修正**：任务表移除勾选和批量分派入口，点行以浅蓝底 / 蓝框选中并同步摘要；头像与编辑笔保留单项安排，完成任务也可选看。恢复原字体设置，容器 / 按钮圆角统一；侧栏、统计卡、项目编辑、新建流程对勾使用 Cloudscape 图标，五格位置条改为浅深蓝轨道。辅助说明补齐任务摘要、分派弹窗、关键节点折叠区、角色职责和预安排，真实状态 / 风险 / 交付要求常显。浏览器验证鼠标及键盘选行、头像不误选、说明开关；390px 模拟页面无横向溢出。
- **KAN-75 样式路径收口（2026-09-24，替代上方旧字体口径）**：恢复 Cloudscape 容器默认外观；`components/ui/tokens.ts` 统一映射官方令牌，页面静态内联样式移入 `styles.css`，响应式布局迁入 `components/ui`。palette 限图表内部；清除旧色值 fallback、占位图 / 缩略图硬编码，收紧自动守卫。字体按 Ryan 新指定改为 Helvetica Neue 优先的完整中英文系统字体栈，移除 Open Sans 加载。任务、关键节点、审核与权限规则未改。
- **本次样式收口验证**：`check_local.py` 六步通过（后端 77、前端 107、工具 17 + 24、构建、diff）；守卫随后补充绕路检测，4 项针对性测试通过。8016 浏览器读取实际计算样式：正文、标题和按钮均为上述 Helvetica 字体栈，修正了别名挂在 html 导致回落到旧默认值的问题。复核选行 / 右侧摘要、点其他任务头像不改变当前摘要、说明与编号开关；关闭说明仍显示等待原因、截止和证据。项目总览、我的事项审核列表、新建首步在 390×844 模拟下无页面级横向溢出，控制台无 error / warn。截图在 `/tmp/kan75-style-contract-shots/`；没有写业务数据、未做真机或线上发布，视觉验收仍待 Ryan。
- **UI 边界与复核入口**：本轮没有后端、权限、任务状态或邮件能力改动，也未写测试房屋业务数据。仍用本地 8016 演示库副本；截图在 `/tmp/kan75-ui-polish-shots/`，清爽 / 说明 / 编号模式可由顶栏开关切换。真机 iPhone、生产部署、邮件未验证；Ryan 视觉与使用反馈待收。当前配色、说明分类和编号维护规则见[界面风格与 AWS 对照](界面风格与AWS对照.md)开头，历史色圈 / 字母标注口径不再适用。

## 2026-09-21 增量核对

- main 前进到 `023ad9a`。09-17 之后合入：PR #13（KAN-40 手机排期重做）、PR #14（KAN-46 工期条）、PR #15（`codex/jev`：项目级 Agent Skills、Codex hooks、品牌与会议素材）、PR #16（KAN-40「状态更新 v1」与 `mgmt-status` 只读接口）。
- Jira 只读核对：KAN-44、KAN-46 已完成；KAN-40 正在进行（真机未验）；KAN-48 的三个提交已在 main 但票仍待办；详见 [Jira 清单](Jira执行清单_2026-09-15.md)。**本次未改任何票的状态。**
- VP 报告站 `/healthz` 返回 200；健康检查不带提交号，**线上是否已跑到 `023ad9a` 未验证**。主业务演示站本次未核对。
- **管理层汇报口径**：只保留 VP 进度报告（`services/vp_report`），数据只来自 Jira。09-18 试过的 Asana 同步汇报已放弃；仓库与本机开发配置中的 Asana 连接已清除，Asana 侧项目待归档、Jira 集成待用户在 Asana 设置里断开。
- 09-21「与 David 对齐」是否已开、结论如何，仓库与 Jira 均无记录，按待确认处理。

## 2026-09-21 增量核对

- `scripts/check_local.py` 现在是**六步**：后端测试、开发工具测试、**脚本工具测试**（新增，按目录发现 `scripts/tests`）、前端单测、前端构建、`git diff --check`。下面 09-17 那条记的「五步」是当时的事实，不改。
- 前端单测从硬编码单文件改成按目录发现 `frontend/src/**/*.test.ts`，当前 4 个文件 49 项（KAN-49）。
- KAN-49 分支上六步全绿。界面非状态色相数改前 5 → 改后 0（默认态），证据在 `docs/reference/ui-audit-2026-09-21/`。
- 新增 `.github/workflows/jira-on-merge.yml`：PR 合入后按 PR 验收表回写 Jira 状态。三个 secret（`JIRA_BASE_URL` / `JIRA_EMAIL` / `JIRA_API_TOKEN`）已由 Ryan 于 21:19–21:20 加好。
  - **已在真实 PR 上触发**：PR #19 合入时跑了一次（run `35656800085`），读到 KAN-40 是「审查中」、在 PR 正文里发现未验证项，于是保持审查中并写评论。没有误标 Done。*本条早先记作「尚未真实触发过」，已作废。*
  - 那一跑暴露两个缺陷，已在 PR #20 修：①标记词匹配到了 PR 正文里对规则本身的描述（散文），改成只搜表格行，拿 PR #19 真实正文回归后命中数 4 → 1；②目标状态等于当前状态时仍会空转一次并写噪声评论，改成跳过。
  - 判定逻辑共 24 条离线单测；另用假 Jira 跑过六种情形（干净 PR、有未验证项、已完成、无票号、缺 secret、转换不存在）。
- **真机 iPhone 仍未验证**，这一项自 09-17 起没有变化。

## 2026-09-17 增量核对

- main 前进到 `9922d5c`（PR #12 合入）。Render 演示站 `/api/health` 实测返回 `{"ok":true,"commit":"9922d5c"}`，线上接口已带 `ws` / `purpose` / `done_when` 字段。
- KAN-44 分支上执行过 `scripts/check_local.py` 五步全绿（后端 20 项、前端单测 31 项、开发工具 17 项、前端构建、`git diff --check`）；前端单测已并入 `check_local.py`。
- CDP 移动端模拟实测 375 / 390 / 430 三个宽度 `scrollWidth == clientWidth`。**真机 iPhone 仍未验证。**
- **本次（文档同步）只改文档，未运行业务测试，未部署，未修改任何 Jira 状态。**

## 2026-09-15 本地验证

- `backend/.venv/bin/python -m unittest discover -s tests`：16 项通过。
- 六段 key、双确认、多门、Final、水电密码和正式模式身份边界已有回归测试。
- 前端完整构建和开发工具检查由 `python3 scripts/check_local.py` 统一执行；每次交付记录当次结果。
- 这些证据不替代真实 iPhone、Cognito、RDS/S3、重新部署或恢复演练。

## 当前风险与归属

| 风险 | 代码事实 | 处理位置 |
|---|---|---|
| 项目授权缺失 | 当前多数 API 仍按角色判断，未建立用户—项目成员关系 | KAN-21 |
| 会话撤销不完整 | 停用用户会拒绝请求；修改密码不会立即使旧签名 Cookie 失效 | KAN-21 验收时处理或明确拆单 |
| 正式配置可误用 | `DEMO_MODE=0` 且未设 `SECRET_KEY` 时只警告；随机密钥会导致重启后全部退出 | KAN-21 / KAN-26 |
| S3 尚未实现 | `STORAGE`、`S3_BUCKET` 只是配置字段 | KAN-23 |
| 数据迁移不足 | 当前用建表和补列兼容，没有正式迁移版本链 | KAN-22 |
| 金额口径不统一 | dashboard 的预计利润采用简化公式，与 analysis 的融资/持有成本口径不同 | 核对现有 Jira 覆盖后再决定是否拆单 |
| 旧步骤与新任务并存风险 | 新任务若另存一套可修改状态，会与清单/总览矛盾 | KAN-27–30 必须使用同源状态 |
| 手机首页只围绕个人待办 | 09-17 会议明确反对；当前 `/todo` 与工作台的组织方式仍是旧口径 | KAN-30 / KAN-31 需调整范围 |
| 跨项目概况缺失 | 没有「一共多少套、各在什么阶段、计划多久/已过多久」的整体视图 | **KAN-56**（待办） |
| 水电日期与资料更新混在一起 | `UtilityAccount` 没有独立的实际开通/关闭日期字段，状态变更与资料编辑共用同一次保存 | **KAN-55**（待办，T4-a） |
| 贷款无处存放 | 数据库里没有贷款实体；`Mortgage` 表属于房产公开记录，不是公司自己的 First/Second Loan | **KAN-51**（待办，T4-b） |
| 保险只是一种文件类型 | 保险现在是 `doc_type='insurance'` 的文件加 `expires_at`，没有类型、状态与切换 | **KAN-52**（待办，T4-c） |
| 采购字段不足 | 现有 `ProcurementItem` 只有波次与状态，没有图片、链接、规格、数量、价格、预计/实际到货 | **KAN-54**（待办，T4-d） |
| 新建项目字段不足 | 没有预计项目周期、土地用途／Zoning；ARV 有字段但不在新建必填路径上 | **KAN-57**（待办，T4-e）；票面只含周期与 Zoning，ARV 未覆盖 |
| 无地图、无中英文 | 均不存在 | 地图 **KAN-58**、中英文 **KAN-53**（均待办，T4-f/T4-g） |
| 本地守卫不是强制门 | Claude hooks 不覆盖其他工具 | 本次新增 CI；合入后由 GitHub 执行后端、守卫和前端检查 |

## 演示和内部使用边界

- `DEMO_MODE=1` 下未登录会按演示负责人处理，可能读取敏感演示字段。只允许合成数据。
- 内部试用必须关闭 demo/seed，固定密钥，使用真实身份与项目授权，并完成 PostgreSQL/S3、部署和恢复验收。
- Render 演示站跟踪 main；合并可能触发部署，但线上健康和提交号需要实际检查。
- AWS、iPhone 与 Jira 状态只按真实记录声明；配置文件、建单或合并本身不等于完成。

## 文档审计结论

活动文档只保留产品方向、阶段目标、实现核验、Jira 执行、完整验收、AWS 和协作约定。2026-09-14 的旧工作流、重复审计、交接稿和过时打印稿已退役，可从 Git 历史 `73a01c2` 读取。仍有效的业务未知集中到[产品方向](产品方向与执行原则.md)，技术风险集中在本页；不再从旧审计批量生成另一套 Jira 结构。
