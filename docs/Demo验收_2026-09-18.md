# 验收脚本

> 文件名里的 `2026-09-18` 是原始命名。本页现在维护**两套**验收：A 是原定 09-18 的技术 demo，B 是 09-17 会议后暂定 09-25 的团队试用。两者目标不同，不要混用。

| | A · 技术 demo | B · 团队试用 |
|---|---|---|
| 日期 | 2026-09-18（**原计划**，已让路） | 2026-09-25（**初步定**，Epic KAN-47） |
| 参与者 | 两台设备、三个测试账号 | 全员到齐，账号由对方同事开 |
| 证明什么 | 任务能分派、手机能提交、负责人能确认、状态一致、越权被拒 | 团队能围绕一套新房子每天协作 |
| 数据 | 合成数据 | 从**一个真实的新项目**开始 |
| 状态 | **待实现、待执行**。09-17 会议未宣布取消，但会议重心已转向 B；范围待重新确认 | **待实现、待执行**，范围与日期均待确认 |

对应 [三个交付 Epic](本周开发Tickets_2026-09-15.md) 与其中的 T4 候选模块。

## A · 技术 demo（原计划 09-18）

## 准备

- 一个可重复访问的测试环境；记录提交、前后端版本、数据库类型、数据是否会重置；仅使用合成资料。
- 两套演示房，用于核对任务实例互不影响。主项目在一个实际演示阶段设 6–8 个任务。
- 负责人、执行者、同角色另一成员各一个测试账号；项目范围预先配置。至少一台电脑和一台 iPhone。
- 代表任务：一次性交付、需要审阅的交付、持续施工更新、等待外部回复、明确前置的任务、不适用任务；另保留 D/J 与 Final 规则的验证场景。
- instruction 写清目的、准备、步骤、证据与完成标准，示例依赖仅用于测试功能，不冒充已确认业务政策。

## 主流程

1. 负责人在电脑登录，查看项目任务，将一项工作派给执行者并设日期、说明与交付要求。
2. iPhone 上执行者登录，从“我的待办”进入该任务，看到相同房屋、日期和说明。
3. 执行者标记等待，填等待对象和原因。负责人刷新后能看到需协调事项。
4. 执行者恢复处理，通过拍照/相册/文件入口提交结果，任务进入待确认。
5. 负责人查看提交物并确认。双方看到一致状态，活动记录保留是谁在何时提交、确认。
6. 展示项目总览中的完成、等待和待确认事实，用已有记录组织一次进度汇报。
7. 改变主项目某任务说明或负责人，第二套项目保持原样。
8. 在 iPhone 将站点添加到主屏幕，退出窗口后从图标启动，验证登录、返回和任务访问。

## 反例必须成立

- 同角色另一成员不会因为角色相同而自动拥有该任务；不在授权项目范围时不能读、写或下载其中资料。
- 在正式模式伪造角色头不能改变身份或看到金额/密码。
- 持续施工上传一次照片只产生更新，整个施工仍未完成。
- 需要证据的任务缺文件时不能完成；待审阅任务不能通过普通状态修改绕过确认。
- 未满足明确前置时保持阻塞；审批节点不能用普通任务“不适用”跳过；最后一次 Final 检查未通过不能确认 Final。
- 上传失败不显示成功；重复点击不会生成重复完成或审批记录。
- 页面刷新或后端进程重启后，已保存任务/附件仍在当前测试存储中；这不替代下周 AWS 重新部署后的持久化验收。

## B · 团队试用 / Demo（初步定 09-25）

**尚未确认的部分不得写成固定验收规则。** 以下场景来自 09-17 会议的明确要求，可直接执行；标「待确认」的条目在业务拍板前只做观察，不作为通过/不通过的依据。

### 环境准备

- 关闭示例数据（`SEED_DEMO=0`），确认库内只有本次要用的真实新项目——**这是环境要求，不是删除现有数据的指令**。
- 参与的人各有独立账号，账号密码登录，手机可访问。
- 记录提交号、环境、数据库类型和是否会重置。

### 主使用路径

1. Jesse（或其助理）新建一个项目，填入地址、ARV／装修后预计售价、预计项目周期等已支持的基础信息。
2. **团队其他账号登录后，能在项目列表里看到这套房，并看到它处于哪个阶段。**（09-17 明确要求：不是只看到自己的几项待办。）
3. 对应职责的人在自己的入口看到该做什么——设计、Permit、采购的人同时能看到公司还有哪些房子、分别在什么阶段。
4. 该负责人更新自己那部分信息（交文件／填数据／记录现场）。
5. **负责人在项目总览看到这条变化**，全程不需要开发者在旁边解释页面。

### 反例必须成立

- 能看到项目名称、地址和阶段，**不等于**能看到金额、凭证和受限文件；未授权项目的敏感字段仍不可见、不可下载。
- 编辑水电账户的邮箱或联系人后，原开通／关闭日期不变，且不产生状态通知。
- 贷款的到期日与相关文件的上传时间是两个值，互不写入。
- 采购里设计师推荐的清单不会自动变成已批准采购；商品页的预计交期不会写进实际到货。

### 待确认，本次只观察不判定

- 关键节点是否必须 D 和 J 双确认、老板未点击是否阻止后续工作（待 David）。
- 线索／洽谈中的房子是否进入平台（待 David）。
- 贷款与保险的提醒时机、提醒谁、谁处理（待 Jesse）。
- 中英文的覆盖范围（待 Jesse）。

### 观察记录

试用当天记录：谁卡在哪一步、哪些信息找不到、哪些词看不懂、哪些事仍然要离开系统去问人。这些是下一轮迭代的输入，不计入通过/不通过。

## 验收记录模板

| 项目 | 执行时填写 |
|---|---|
| 本次验收是 A 还是 B | 待填写 |
| commit / 环境 / 时间 | 待填写 |
| 电脑浏览器 | 待填写 |
| iPhone 型号 / iOS / Safari / 主屏幕模式 | 待填写 |
| 三种手机宽度检查 | 待填写 |
| 主流程结果与证据位置 | 待填写 |
| 反例结果 | 待填写 |
| 测试命令与结果 | 待填写 |
| 尚未验证项 / 阻塞 | 待填写 |

只有实际执行过的项目才能标通过。桌面视口模拟可检查排版，不能替代 iPhone 上的文件选择、会话和主屏幕验证。

Apple 官方支持在 iPhone Safari 将网站添加到主屏幕并作为网页应用打开；具体操作和界面随系统版本，以测试设备为准。[Apple 操作说明](https://support.apple.com/en-euro/guide/iphone/iphea86e5236/ios)

---

# 例行体验测试记录 · 2026-09-21

本节是一次 30 分钟限时的**体验测试**记录，不是新的验收标准，也不改上面的脚本。
结论只覆盖本次实测到的范围；没测到的一律标「未测」，不按「大概没问题」写成通过。

**后续复核说明**：第 10 节补充了 P4 的成功复测及验收口径修正。以下保留首轮记录；“有条件通过”仅限其中已经实测的演示功能，不代表正常用户全流程或正式试用验收通过。

## 1. 实测版本与环境

| 项 | 值 |
|---|---|
| 线上 commit | `ca03b69`（`/api/health`），与 `origin/main` 一致 |
| 环境 | Render staging，`DEMO_MODE=1`、合成种子数据、临时磁盘 |
| 登录状态 | `has_users: false` —— **一个账号都没有**，全程靠演示身份切换（`X-Actor` 头 / `localStorage.actor`） |
| 本地分支 | `codex/role_testing`，工作区干净，**本次未改任何 backend/frontend 代码** |
| 设备 | 桌面 Chrome；窄屏 390 / 430 用同源 iframe 量。**真 iPhone 未使用** |
| 测试房 | 项目 11「【测试】Parkville 合成测试房」，合成地址，J 身份创建 |
| 用时 | 18:10:08 → 18:21:23（测试段 11 分钟） |

**这一条决定了下面所有权限结论的性质**：演示模式下任何人只要改一个请求头就能变成任何角色，
所以本节只能说「**规则按角色代号生效**」，**不能**说「账号权限验收通过」。

## 2. 角色覆盖与结果

14 个角色全部检查了工作台与项目详情入口：**全部 HTTP 200，无人被挡在门外**。

| 角色 | 层级 | 工作台小组件 | 我的待办 | 待我确认 | 金额 |
|---|---|---|---|---|---|
| 老板 | 决策 | 全部 | — | 11 | 可见 |
| D / J | 决策 | gates 等 | — | 11 | 可见 |
| 负责人 | 统筹 | 全部 | — | 11 | 可见 |
| L | 统筹 | mytodo 等 | **11 条** | — | 可见 |
| PM | 统筹 | mytodo 等 | 4 条 | — | 可见 |
| K | 执行 | mytodo + 水电 | 4 条 | — | 隐藏 |
| Z | 执行 | mytodo + permit | 3 条 | — | 隐藏 |
| S / W | 执行 | mytodo + 卖房文件 | 各 1 条 | — | 隐藏 |
| A / 设计师 | 执行 | mytodo | 0 条 | — | 隐藏 |
| 园丁 / 承包商 | 外部 | 只有 mytodo | 0 条 | — | 隐藏 |

`money_hidden` 的分界线与 `read_money: ['purple','blue']` 完全一致，**执行级和外部角色看不到金额**。

**决策级角色没有「我的待办」**（`DASHBOARD_LAYOUTS` 里老板/D/J/负责人 配的是 `gates` 不是 `mytodo`）。
J 是「筛选房源」的负责人，但 J 的待办数是 0——J 自己的活只能在项目页里翻。归「业务规则待确认」。

## 3. 通过 / 失败 / 未测

**通过（实测）**
- 新建项目保存后可重新找回，地址、买入价、目标售价一致。
- **新项目自动进入交接链**：L 收到 2 条待办（看房、签 loan doc），J/D/负责人/老板 各收到 2 条待确认。
- **D/J 双确认规则不变**：D 单独确认 → `confirmed=['D'] waiting=['J']`，节点仍未过；J 再确认 → `done=True`。
- 文件上传落到正确项目，刷新后仍在，负责人能看到**内容 + 上传角色 + 时间**（设计师/PM/S 三份都对）。
- **单家水电开通不等于三家全开**：电=on，水/瓦斯仍 not_started，老板工作台三家分开显示。
- 检查未通过如实记录（result=failed、整改人 PM），老板工作台的工地块显示 `failed: ['【测试】电路阶段性检查']`。
- 采购异常如实记录并上浮到老板的采购告警块。
- 负责人保存的风险文本正确存回。
- 窄屏 390 / 430 **无横向滚动**（窄屏模拟，非真机）。

**失败 / 阻断（实测）**——详见第 4 节
- W 打不开 S 提交的卖房文件。
- 老板主列表把刚开 escrow 的房子显示成「施工中」。
- 水电账号与登录名对外部角色可见。

**未测**
- **真 iPhone**：Safari 文件选择、键盘遮挡、主屏幕模式——无设备，全部未测。
- **账号登录与真实权限**：线上没有任何账号，登录路径、会话、越权全部未测。
- 承包商是否正式进平台——仍是待确认业务规则，未测。
- 界面新建项目向导：地址候选选不中（见 P4），**未走通**。
- A → 园丁 剪草交接：园丁/A 当前待办为 0（该步在 ⑤ 卖房上市，测试房还没走到），未测。

## 4. 问题清单

最影响正常使用的是前三条。

### P1 ｜老板主列表把「刚开 escrow」显示成「施工中」｜严重程度：高

| 项 | 内容 |
|---|---|
| 角色/设备 | 老板 / 桌面 Chrome |
| 项目 | 11【测试】Parkville 合成测试房 |
| 复现 | 新建项目 → D 和 J 都确认 `open_escrow` → 打开工作台看项目列表 |
| 期望 | 显示「② 买房与过户」这类与实际进度相符的阶段 |
| 实际 | 列表写「**在建 · 施工中**」；同一项目的六阶段清单写「② 买房与过户」，`close_escrow`（过户）`done=False`、`start`（可以开工）`done=False` |
| 根因（代码观察） | `backend/app/dictionaries.py:281` 的 `STAGE_TO_LEGACY` 把 `s2`（买房与过户）直接映射为 `("active","construction")` |
| 业务影响 | 老板一眼读到的阶段是错的：房子还没过户，列表说在施工。直接违反「老板能在一分钟内看懂当前阶段」 |
| 证据 | `GET /api/projects/11` → `stage=active, substage=construction`；`GET /api/projects/11/steps` → `current_stage=② 买房与过户` |
| 分类 | 已实现功能的缺陷 |
| 关联 Jira | 未查到 |

附带同一处：该项目有失败检查和采购异常，列表「状态」列仍写「**正常**」。

### P2 ｜W 打不开同为负责人的 S 提交的卖房文件｜严重程度：中

| 项 | 内容 |
|---|---|
| 角色 | S（上传）→ W（接手） |
| 事项 | ⑤ 卖房上市 · 卖房文件（`sale_docs`，**owners = ["S","W"]**，两人同为负责人） |
| 复现 | S 上传 `doc_type=sale_docs` 的文件 → 切换到 W → 文件列表能看到 → 点下载 |
| 期望 | W 作为同一步骤的负责人能接着处理 |
| 实际 | 列表可见，**下载 HTTP 403** |
| 根因（代码观察） | `backend/app/routers/files.py:38-46`：青级只有 `doc_type not in MONEY_DOCS` 才能下载，而 `sale_docs ∈ MONEY_DOCS`（`dictionaries.py:335`）。S 能下载只是因为「自己传的」 |
| 业务影响 | S→W 这条交接在系统内断掉，必须让负责人或紫蓝角色代为转交 |
| 分类 | **交接阻断 + 业务规则待确认**——是「按规则工作」，但业务上交接不成立。规则本身要 David 定 |
| 关联 Jira | 未查到 |

### P3 ｜水电账号与登录名对外部角色可见｜严重程度：高

| 项 | 内容 |
|---|---|
| 角色 | 园丁、承包商（外部），以及 Z/S/设计师（执行） |
| 复现 | K 保存电力账户（含账号、登录名、密码）→ 切换成园丁 → `GET /api/projects/11/utilities` |
| 期望 | 外部角色看不到账户凭证类字段 |
| 实际 | `password` 已置空 ✅，但 `account_no='TEST-0001'`、`login='testuser'`、`company`、`opened_under` **全部可见** |
| 根因（代码观察） | `backend/app/routers/ops.py:44` 只对 `password` 做 `model_copy(update={"password": None})` |
| 业务影响 | 公司名 + 账号 + 登录名足以尝试账户找回或社工。验收标准原文只写了「水电密码」，**字面上算通过**，但凭证边界显然不止密码 |
| **口径澄清** | 演示模式下 `has_users:false`，谁都能把 `X-Actor` 改成任何角色——**本次观察本身不构成「外部人员看到了凭证」**，因为压根没有外部人员的会话。这条记的是**规则缺口**：`utility_secret` 只挡了 `password`，等 KAN-21 把真实账号和成员接上，灰级用户就会真的看到这四个字段。判高是因为它会**跟着 KAN-21 一起上线**，要在那之前补 |
| 分类 | 当前功能缺口 + 业务规则待确认 |
| 关联 Jira | 未查到（KAN-55 是水电**日期与通知**，不含字段可见性） |

### P4 ｜新建项目向导：地址候选点不中｜严重程度：未定（需人工 30 秒复核）

界面上输入地址后候选正确弹出，但两次合成事件点击 + 两次可信点击（含官方种子地址 Parkville）
都停在「请先选择一个地址并等待数据补全」，输入框值不变、下拉不关。
**我无法区分这是浏览器自动化的信任事件限制还是真缺陷**，所以不判缺陷也不判通过。
请人工在界面上点一次：能选中就是自动化问题，选不中就是**高**（新建项目主路径阻断）。
本轮的测试项目是绕过界面、用 `POST /api/projects` 建的——**接口正常**。

### P5 ｜外部角色可下载的判据只看 MIME，不看文件类型｜严重程度：中

`files.py:41-44`：外部（灰级）角色的可见/可下载判据是「是不是 image/*」。
本轮 S 传的卖房文件是 PNG，园丁**下载成功（200）**。
真实场景里合同、结算单常常是手机拍的照片或扫描件，按现在的规则会对外部角色开放。
这是**规则口径问题**，不是我用错类型造成的。

### P6 ｜上传接口不校验 doc_type｜严重程度：低｜接口观察，界面未复现

`files.py:71` 校验了 `step_key`，但 `doc_type` 没有对照 `FILE_TYPES`。
我用 `doc_type=site_photo`（不存在的类型）上传成功，文件存下了，但老板工作台的工地块
`photo_count` 仍是 0——文件成了孤儿，也不会满足任何 `file:` 证据规则。
**界面上是下拉选择，普通用户碰不到**，所以标「接口观察」。

### P7 ｜决策级角色没有「我的待办」｜严重程度：低｜业务规则待确认

`dictionaries.py:342-345`：老板/D/J 的布局里只有 `gates`，没有 `mytodo`。
J 是「筛选房源」「房屋检查」「分阶段采购」等步骤的负责人，但 J 的待办列表是 0 条。
是有意设计（决策只看门）还是漏配，要 David 确认。

## 5. 六阶段「动作—下一位—提醒」简表

**实测链路**与**仅设计推演**分开。步骤 key 与负责人来自 `dictionaries.py:233-276`，不是推测。

### 5.1 实测链路（本轮真的跑过）

| 阶段 | 动作（step key） | 谁做 | 完成算什么 | 下一位 | 对方怎么知道 | 要不要提醒 |
|---|---|---|---|---|---|---|
| ① 预买房 | 项目创建 | J | 项目存在 | L（看房）、D/L（算账定价） | **进 L 的「我的待办」** ✅ 实测 | **要**·Email·给 L |
| ① 预买房 | `open_escrow` gate | D 和 J **各确认一次** | 两人都确认才过 | 只差谁就催谁 | 进 D/J 的「待我确认」✅ 实测 | **要**·只发给**尚未确认**的那个人 |
| ② 买房与过户 | `design` 上传方案 | 设计师 | 传了 `drawing` | 负责人查看 | 负责人在文件页看到内容+角色+时间 ✅ 实测 | **要**·Email·给负责人 |
| ③ 装修 | `progress` 施工照片 | PM | 传了照片 | 负责人 | 同上 ✅ 实测 | **不要**——一张进度照片不代表整段施工验收完成，只进汇总 |
| ③ 装修 | `inspections` 检查未通过 | Z | 记 `result=failed` + 整改人 | **整改人（本例 PM）**，负责人协调 | 老板/负责人工地块显示 failed ✅ 实测；**整改人本人无任何通知** ❌ | **要**·SMS·给整改人（阻塞下一步） |
| ③ 装修 | 采购转 `exception` | J | 状态=exception | 负责人 / PM（影响开工） | 老板采购告警块 ✅ 实测 | **要**·Email·给负责人 |
| ②→⑥ | `utilities` 电=on | K | 状态变 on | 负责人 | 老板水电块三家分列 ✅ 实测 | **要**·Email·**只在 on/off 状态变化时**；改公司/账号/联系人**不发**（`本周开发Tickets:129-134`） |
| ⑤ 卖房上市 | `sale_docs` 上传 | S | 传了 `sale_docs` | **W（同为该步负责人）** | W 看得到、**打不开（403）** ❌ 实测 P2 | **要**·Email·给 W——但**提醒点进去会撞 403**，先定权限再做提醒 |

### 5.2 仅设计推演（本轮未跑到）

| 阶段 | 动作 | 谁做 | 下一位 | 要不要提醒 | 备注 |
|---|---|---|---|---|---|
| 各处 | 编辑资料（公司/账号/联系人/备注） | 任意有权角色 | 无 | **不要**·普通资料编辑，不产生新待办 | **本轮未实测**：没有单独 PATCH 过公司/联系人再验证「无状态事件」。文档规则见 `本周开发Tickets:129-134` |
| ② | `close_escrow` gate | D+J | K（开水电） | 要·只催未确认者 | 未测 |
| ③ | `permit_apply` / `permit_issued` | Z | PM（可以开工） | 要 | 未测 |
| ③ | `start` gate | D+J | PM | 要·只催未确认者 | 未测 |
| ③ | `final` gate | D+J | J（staging） | 要 | **唯一有硬前置**：必须先有通过的 final 检查 |
| ④ | `listing` gate | J | S/W | 要 | **唯一没有 D/J 双确认的 gate**，只看证据 |
| ⑤ | `mow` 剪草 | A、园丁 | A 验收 | 要——**但园丁没有账号**，怎么送达是设计问题 | 未测 |
| ⑤ | `offer` / `closed` gate | D+J | S/W/K | 要·只催未确认者 | 未测 |
| ⑥ | `services_off` 关水电、退保险 | K | 收尾 | 要·状态变化才发 | 未测 |
| 全流程 | 等外部回复（贷款、permit、offer） | — | 内部跟进人 | 待定 | **系统里没有「等待外部」这个状态**，也没有到期日 |

## 6. 下一步行动提醒 · 第一版建议范围

**本节是需求草稿，不是已确认规则，也不是已实现功能。本轮没有发送任何真实短信或邮件。**

### 6.1 硬依赖（这三条不解决，提醒发不出去）

| 依赖 | 现状（代码/Jira 实测） |
|---|---|
| **提醒要发给具体人** | 步骤上**只有角色代号**，没有 user_id。`ProjectStep.done_by` 是自由字符串（`models.py:226`）。**KAN-21**（账号、项目权限与可分派成员接口）状态**待办**，目标 09-22 |
| **要有联系方式** | `User` 模型（`models.py:281-294`）**没有 email、没有 phone**。仓库里的 phone/email 属于 `Owner`（业主），不是系统用户 |
| **要有发送能力** | backend 全库 **没有任何邮件/短信/推送能力**：smtp / sendgrid / twilio / SES / boto3 **全部未查到**，`requirements.txt` 里也没有 |

结论：**提醒功能目前是零基础**，且第一条硬依赖挂在 KAN-21 上。

### 6.2 还缺的数据结构

| 缺口 | 影响 |
|---|---|
| 步骤/任务上**没有到期日**（全库唯一到期日是 `ProjectFile.expires_at`） | 「到期未处理再提醒」「临近截止发 SMS」目前**无从触发** |
| `UtilityAccount` **没有实际开通/关闭日期**，只有 `status` + `updated_at` | 业务事实日期与维护时间没分开 —— 正是 **KAN-55**（待办）要做的 |
| `ProcurementItem` **没有实际到货日期** | 同上，且「到货影响后续工作」无法按日期触发 |
| 没有「等待外部回复」状态 | 「等外部时内部由谁跟进」无处挂 |
| 没有通知表 / 发送记录 | 「已发送 / 送达 / 已处理」三种状态无处存，失败无法追踪 |

### 6.3 建议范围（第一版）

只做**实测链路**那 8 条事件，每条按七元组定义：
业务事件 → 接收人 → 要做什么 → 触发时机 → 渠道 → 点击目的地 → 何时停止。

**渠道建议（未确认）**：Email 用于普通交接、资料审阅、汇总；SMS 只用于明确阻塞下一步的事项
（当前只有「检查未通过 → 整改人」明确属于这一类）。

**必须内建的约束**（对应验收要点，逐条可测）：
1. 收件人解析不到具体人 → 记「**未能提醒**」并写明原因，**不显示已通知**。
2. 有手机号 ≠ 允许发短信，渠道偏好与短信接收授权分开存。
3. 普通字段修改不触发状态通知；业务日期与资料更新时间分开判断。
4. 同一事件 + 同一接收人 + 同一状态，刷新/重复保存/重试**只产生一条**（幂等键建议 `事件类型:项目:步骤:接收人:状态版本`）。
5. 多人审批**只催未处理的人**（`open_escrow` 的 `waiting` 字段已经算好了，直接用）。
6. 「已发送 / 送达 / 已处理」三个独立状态；发送失败可追踪，不得显示已送达。
7. 链接指向具体项目+事项，**登录后再校验一次访问权限**——P2 已经证明「看得到 ≠ 打得开」。
8. 消息只带处理所需最少信息：**不带密码、不带敏感金额、不带附件原件**。
9. 事项完成/取消/转交/接收人变化 → 旧提醒停止。
10. 并行多条 → 按人按项目合并成一条摘要，避免轰炸。

**待决定参数（不擅自定）**：提前多久、重发频率、静默时段、升级找谁、外部人员（园丁/承包商）无账号时怎么送达。
文档已记明贷款提醒时机「待 Jesse」（`产品方向与执行原则.md:48`），本节沿用，不写成已确认。

### 6.4 模拟文案（示例，未发送）

**Email —— 检查未通过，通知整改人**
> 主题：【Parkville 57th Terr】电路阶段性检查未通过，需要你整改
>
> PM，你好：
> 9/21 Z 记录了一次检查未通过：电路阶段性检查 —— 接地不合格。
> 需要你做：完成整改并回传整改说明或照片，然后通知 Z 约复检。
> 这一步没完成，「final（City 验收通过）」过不了。
> 打开事项：https://…/projects/11?tab=overview&step=inspections
> （本邮件不含任何账号密码或金额。）

**SMS —— 只催尚未确认的那一位**
> 【翻新平台】Parkville 57th Terr：Open escrow 需要你确认，D 已确认，等你。打开：https://…/p/11/s/open_escrow

## 7. 给领导的简短反馈

> 今天用一套合成测试房把流程从建项目走到卖房文件，跑了 11 分钟。
>
> **好的地方**：新建项目之后活会自动落到人头上——L 立刻收到 2 条待办，D、J、老板收到待确认。
> D/J 双确认规则完全正确：一个人确认不算过，两个人都确认才过。
> 各角色提交的东西（设计方案、施工照片、水电状态、检查记录、采购异常）都能被负责人和老板看到，
> 带着是谁、什么时候。金额对执行和外部人员是隐藏的。手机宽度下没有横向滚动。
>
> **三个要先解决的**：
> 1. 老板主列表把刚开 escrow、还没过户的房子显示成「施工中」，一眼看过去阶段是错的。
> 2. S 传的卖房文件，W 看得到但打不开——这两人是同一件事的负责人，交接在系统里断了。
> 3. 水电账户的密码挡住了，但公司名、账号和登录名对园丁、承包商这些外部角色是可见的。
>
> **提醒功能**：今天只做了设计，没有开发，也没发任何短信邮件。
> 要说清楚一件事——**现在还发不出去**：系统里一个账号都没有，用户表上没有邮箱和手机号，
> 后端也没有任何发短信发邮件的能力。而且步骤上只有「角色 K」，没有「具体是谁」。
> 所以提醒的第一步不是写发送代码，是先把账号和成员接上（KAN-21，现在还是待办）。
>
> **本轮结论：有条件通过。** 条件是——演示模式、没有账号、没有真机验证。
> 这三样不补上，今天的权限结论只能算「规则按角色生效」，不能算账号权限验收通过。

## 8. 本次发现的文档口径冲突（需更正，本轮未改）

AGENTS.md 与 `docs/本周开发Tickets_2026-09-15.md:127`、`docs/Jira执行清单_2026-09-15.md:106-108`
都还写着「水电日期与通知**未建单**」，但 Jira 里 **KAN-55**（水电气：实际开通/关闭日期与资料更新分离，
通知只在状态变化时发）**已经存在**，状态待办，`ROADMAP.md:41` 也已经写了 KAN-55。
这两处是旧口径，建议单独更正，本轮没有改。

同时确认：Jira 里 **没有**专门的「下一步行动提醒」票，summary 含 SMS / notification / reminder 的票也**未查到**。
本轮**没有新建或修改任何 Jira**。

## 9. 本轮在 staging 留下的测试数据

项目 11「【测试】Parkville 合成测试房」及其 3 个测试文件、1 条失败检查、1 条采购异常、
1 条电力账户（密码是 `TEST-PW-DO-NOT-USE`，非真实凭证）、1 条风险文本。
全部合成。Render 是临时磁盘，重部署即消失；如需立刻清掉，删项目 11 即可。

## 10. Codex 后续复核（同一版本 ca03b69）

### P4：本次候选选择成功，原失败原因仍未确定

- 环境：线上 `/api/health` 返回 `ca03b69`；Codex 内置桌面浏览器，演示身份为负责人。
- 操作：打开 `/projects/new` → 在“房产地址”输入 `Parkville` → 等候选出现 → 点击 `10404 NW 57th Terr, Parkville, MO 64152` → 点击“下一步”。
- 实际：输入框更新为完整地址、下拉关闭，显示重复房产提示及 APN/估值/挂牌价；随后成功进入“确认房产数据”，显示补全 16 项。
- 结论：**这条地址选择路径本次通过，未复现 P4**。不同浏览器、身份及工具可能影响结果，本次证据不足以断言首轮失败一定是工具问题，也不排除间歇性问题。无需再把人工复核作为整轮测试的阻塞项。
- 范围：未点击最终“创建项目”，未新增测试房；完整网页创建、J 身份和真 iPhone 路径仍未完成复测。

### 验收结论和优先级修正

1. 首轮通过项中的“新建项目”实际通过 API 完成；14 角色 HTTP 200 只证明接口可达，不能等同于 14 个角色都能从界面找到并完成工作。待办出现在角色视图，也不代表已分派给具体账号或已发送提醒。
2. 给领导的口径应为：**演示环境下的部分业务操作与角色规则已跑通；正常用户全流程验收尚未完成，正式试用未通过验收。** 缺少真实账号/非演示环境/真机是未验证范围，P1、P2、P3、P5 则是另行需要处理的已记录问题。
3. **P3 与 P5 应共同列为接入真实资料前的高优先级问题。** P5 的 MIME 图片判据不能替代文件业务权限；首轮记录了园丁下载卖房 PNG 成功，代码 `_can_download` 也印证此路径。本次仅复核代码，没有重复下载。水电账号、登录名应明确受控；公司名等字段的可见性逐字段决定，不能一概等同密码。更新记录中的同类信息也应纳入边界检查。
4. P1 的错误阶段映射是明确缺陷；“正常”状态不计入失败检查/采购异常是另一项状态汇总口径问题。当前 `compute_status` 只考虑人工覆盖、阶段、预算及计划完工日，需分别记录、验收。
5. P2 需要明确 S/W 对特定项目、特定事项文件的协作权限，不能通过开放全部财务文件解决。具体确认人按现有权限矩阵约定与 Jesse 对齐；只有需要 David 决策的部分再提交。
6. 提醒依赖不止 KAN-21：账号/项目成员由 KAN-21 支撑，具体任务实例、分派及到期日还涉及 KAN-27/28。联系方式、通知偏好、事件触发与发送记录需另行明确范围；即时交接提醒不必等待逾期提醒实现。
7. “检查失败即阻塞下一步、直接发 SMS”只可作为待确认建议，不能成为新增硬前置；应区分普通检查、最近一次 Final 检查及真正受影响的工作。取得 permit 也不自动等于可以开工，仍保留当前开工确认规则。

本次只补充本节证据与解释，未修改业务代码、Jira 或发送消息，未提交文档。


## 11. 建单结果（2026-09-21）

本次把第 4 节的问题与第 10 节的复核整理进 Jira。**查重后未发现已有执行单覆盖，六张全部新建，未新建 Epic。**

| 报告编号 | 票号 | 类型 | 优先级 | 父 Epic | 能否直接开工 |
|---|---|---|---|---|---|
| P1 阶段映射 | **KAN-65** | 任务 | High | KAN-47 | **可以**（次要取值待确认，不阻塞主体） |
| P1 状态口径 | **KAN-66** | 任务 | Medium | KAN-47 | 否——口径待负责人确认 |
| P2 S→W 文件 | **KAN-67** | 任务 | High | KAN-47 | 否——授权规则待 Jesse 确认 |
| P3 水电字段 | **KAN-68** | 任务 | High | KAN-35 | 否——字段分层待 Jesse 确认 |
| P5 图片绕过 | **KAN-69** | 任务 | High | KAN-35 | 部分——主判据清楚，需与 KAN-67 统一 |
| P4 地址选择 | **KAN-70** | 任务 | Medium | KAN-47 | **可以**（调查不需要前置决定） |

票间关系：KAN-65 ↔ KAN-66（relates，同源但分开修）；KAN-67 ↔ KAN-69（relates，**必须共用同一套授权判定**）；
KAN-68、KAN-69 **blocks** KAN-21（要赶在真实账号接通前完成）；KAN-68 ↔ KAN-55（relates，范围不重叠：KAN-55 只做日期与通知）。

**P6（上传接口不校验 doc_type）与 P7（决策级角色没有「我的待办」）本轮未单独建单**：
P6 只在直接调接口时可触发、界面未复现，已写进 KAN-69 的实施注意事项；P7 是待确认的业务规则，不是缺陷。

严重程度与票号是两套编号，P1–P5 只是本报告内的问题编号，不要当票号用。
